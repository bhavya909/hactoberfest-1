#include<stdio.h>
int factorial(n)
{
    if (n==0 ||n==1)
    {
        return 1;
    }
    else
    {
        return n*factorial(n-1);
    }
}
int main()
{

    int c,d;
    c=factorial(5);

    d=factorial(6);
    printf("%d",c);
    printf("\n");
    printf("%d",d);
    return 0;
}
